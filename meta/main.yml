---
galaxy_info:
  author: Bruce Becker
  description: Ansible role for the BDII (Top,Site or Resource)
  company: EGI Foundation
  license: Apache-2.0
  min_ansible_version: 2.5
  platforms:
    - name: EL
      versions:
        - 6
        - 7
    - name: Debian
      versions:
        - 8
        - 9
        - 10
  galaxy_tags:
    - openldap
    - slapd
    - information
    - EGI
    - grid
dependencies:
  - brucellino.ansible-umd
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
